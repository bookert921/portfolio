/**
 * Interact with client side of Gatsby.
 * CSS files with global styles like
 * typography and colors are typically
 * imported into the site’s
 * gatsby-browser.js file.
 * */

// import Layout from "@components/Layout";
import "@fontsource/bitter";
import "@fontsource/cabin-condensed";
import "@fontsource/condiment";

// // Logs when the client route changes
// exports.onRouteUpdate = ({ location, prevLocation }) => {
//   console.log("new pathname", location.pathname);
//   console.log(
//     "old pathname",
//     prevLocation ? prevLocation.pathname : null
//   );
// };

// // Wraps every page in a component
// export function wrapPageElement({ element, props }) {
//   return <Layout {...props}>{element}</Layout>;
// }
